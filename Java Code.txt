import java.sql.*;
import java.util.Scanner;

public class SchoolManagementSystem {
    // JDBC URL, username, and password
    static final String URL = "jdbc:mysql://localhost:3306/lotus";
    static final String USER = "root";
    static final String PASSWORD = "";
    
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        try (Connection conn = DriverManager.getConnection(URL, USER, PASSWORD)) {
            System.out.println("✅ Database Connected Successfully!");
             
            int mainChoice;
            
            do {
                System.out.println("\n===== MAIN MENU =====");
                System.out.println("1. Student Menu");
                System.out.println("2. Teacher Menu");
                System.out.println("3. Staff Menu");
                System.out.println("4. Exit");
                System.out.print("Enter your choice: ");
                mainChoice = sc.nextInt();
                
                switch (mainChoice) {
                    case 1: studentMenu(conn, sc); break;
                    case 2: teacherMenu(conn, sc); break;
                    case 3: staffMenu(conn, sc); break;
                    case 4: System.out.println("👋 Exiting..."); break;
                    default: System.out.println("❌ Invalid choice. Try again.");
                }

            } while (mainChoice != 4);

        } catch (SQLException e) {
            e.printStackTrace();
        }
    }
     
     private static void studentMenu(Connection conn, Scanner sc) throws SQLException {
        int choice;
        do {
            System.out.println("\n===== STUDENT MENU =====");
            System.out.println("1. Add Student");
            System.out.println("2. View Students");
            System.out.println("3. Update Student");
            System.out.println("4. Delete Student");
            System.out.println("5. Exit");
            System.out.print("Enter your choice: ");
            choice = sc.nextInt();

            switch (choice) {
                case 1: addStudent(conn, sc); break;
                case 2: viewStudents(conn); break;
                case 3: updateStudent(conn, sc); break;
                case 4: deleteStudent(conn, sc); break;
                case 5: System.out.println("⬅ Existing...."); break;
                default: System.out.println("❌ Invalid choice");
            }
        } while (choice != 5);
    }
     
    private static void addStudent(Connection conn, Scanner sc) throws SQLException {
        System.out.print("Enter name: ");
        sc.nextLine(); // consume newline
        String name = sc.nextLine();
        System.out.print("Enter age: ");
        int age = sc.nextInt();
        System.out.print("Enter course: ");
        sc.nextLine();
        String course = sc.nextLine();

        String sql = "INSERT INTO student (name, age, course) VALUES (?, ?, ?)";
        try (PreparedStatement ps = conn.prepareStatement(sql)) {
            ps.setString(1, name);
            ps.setInt(2, age);
            ps.setString(3, course);
            int rows = ps.executeUpdate();
            System.out.println(rows + " student(s) added.");
        }
    }
    
    private static void viewStudents(Connection conn) throws SQLException {
        String sql = "SELECT * FROM student";
        try (Statement stmt = conn.createStatement();
             ResultSet rs = stmt.executeQuery(sql)) {
            System.out.println("\nID\tName\tAge\tCourse");
            System.out.println("-----------------------------------");
            while (rs.next()) {
                System.out.println(rs.getInt("id") + "\t" +
                        rs.getString("name") + "\t" +
                        rs.getInt("age") + "\t" +
                        rs.getString("course"));
            }
        }
    }
    
    private static void updateStudent(Connection conn, Scanner sc) throws SQLException {
        System.out.print("Enter Student ID to update: ");
        int id = sc.nextInt();
        sc.nextLine();
        System.out.print("Enter new name: ");
        String name = sc.nextLine();
        System.out.print("Enter new age: ");
        int age = sc.nextInt();
        sc.nextLine();
        System.out.print("Enter new course: ");
        String course = sc.nextLine();

        String sql = "UPDATE student SET name=?, age=?, course=? WHERE id=?";
        try (PreparedStatement ps = conn.prepareStatement(sql)) {
            ps.setString(1, name);
            ps.setInt(2, age);
            ps.setString(3, course);
            ps.setInt(4, id);
            int rows = ps.executeUpdate();
            System.out.println(rows + " student(s) updated.");
        }
    }
    
    private static void deleteStudent(Connection conn, Scanner sc) throws SQLException {
        System.out.print("Enter Student ID to delete: ");
        int id = sc.nextInt();

        String sql = "DELETE FROM student WHERE id=?";
        try (PreparedStatement ps = conn.prepareStatement(sql)) {
            ps.setInt(1, id);
            int rows = ps.executeUpdate();
            System.out.println(rows + " student(s) deleted.");
        }
    }
    
     private static void teacherMenu(Connection conn, Scanner sc) throws SQLException {
        
        int choice2;

        do {
           System.out.println("\n===== TEACHER MENU =====");
           System.out.println("1. Add Teacher");
           System.out.println("2. View Teachers");
           System.out.println("3. Update Teacher");
           System.out.println("4. Delete Teacher");
           System.out.println("5. Exit");
           System.out.print("Enter your choice: ");

        choice2 = sc.nextInt();

        switch (choice2) {
            case 1: addTeacher(conn, sc); break;
            case 2: viewTeachers(conn); break;
            case 3: updateTeacher(conn, sc); break;
            case 4: deleteTeacher(conn, sc); break;
            case 5:
                System.out.println("⬅ Existing");
                break;
            default:
                System.out.println("❌ Invalid choice. Try again.");
        }
    } while (choice2 != 5);
}

    

  
    
    private static void addTeacher(Connection conn, Scanner sc) throws SQLException {
        System.out.print("Enter name: ");
        sc.nextLine(); // consume newline
        String name = sc.nextLine();
        System.out.print("Enter subject: ");
        String subject = sc.nextLine();
        System.out.print("Enter salary: ");
        sc.nextLine();
        int salary = sc.nextInt();

        String sql = "INSERT INTO teacher (name, subject, salary) VALUES (?, ?, ?)";
        try (PreparedStatement ps = conn.prepareStatement(sql)) {
            ps.setString(1, name);
            ps.setString(2, subject);
            ps.setInt(3, salary);
            int rows = ps.executeUpdate();
            System.out.println(rows + " teacher(s) added.");
        }
    }
    
    private static void viewTeachers(Connection conn) throws SQLException {
        String sql = "SELECT * FROM teacher";
        try (Statement stmt = conn.createStatement();
             ResultSet rs = stmt.executeQuery(sql)) {
            System.out.println("\nID\tName\tSubject\tsalary");
            System.out.println("-----------------------------------");
            while (rs.next()) {
                System.out.println(rs.getInt("id") + "\t" +
                        rs.getString("name") + "\t" +
                        rs.getString("subject") + "\t" +
                        rs.getInt("salary"));
            }
        }
    }

    private static void updateTeacher(Connection conn, Scanner sc) throws SQLException {
        System.out.print("Enter teacher ID to update: ");
        int id = sc.nextInt();
        sc.nextLine();
        System.out.print("Enter new name: ");
        String name = sc.nextLine();
        System.out.print("Enter new subject: ");
        String subject = sc.nextLine();
        sc.nextLine();
        System.out.print("Enter new salary: ");
        int salary = sc.nextInt();

        String sql = "UPDATE teacher SET name=?, subject=?, salary=? WHERE id=?";
        try (PreparedStatement ps = conn.prepareStatement(sql)) {
           ps.setString(1, name);
           ps.setString(2, subject);
           ps.setInt(3, salary);
           ps.setInt(4, id);
            int rows = ps.executeUpdate();
            System.out.println(rows + " teacher(s) updated.");
        }
    }

    private static void deleteTeacher(Connection conn, Scanner sc) throws SQLException {
        System.out.print("Enter Teacher ID to delete: ");
        int id = sc.nextInt();

        String sql = "DELETE FROM teacher WHERE id=?";
        try (PreparedStatement ps = conn.prepareStatement(sql)) {
            ps.setInt(1, id);
            int rows = ps.executeUpdate();
            System.out.println(rows + " teacher(s) deleted.");
        }
    }
    


    private static void staffMenu(Connection conn, Scanner sc) throws SQLException {
        
        int choice2;

        do {
           System.out.println("\n===== STAFF  MENU =====");
           System.out.println("1. Add Staff");
           System.out.println("2. View Staffs");
           System.out.println("3. Update Staff");
           System.out.println("4. Delete Staff");
           System.out.println("5. Exit");
           System.out.print("Enter your choice: ");

        choice2 = sc.nextInt();

        switch (choice2) {
            case 1: addStaff(conn, sc); break;
            case 2: viewStaffs(conn); break;
            case 3: updateStaff(conn, sc); break;
            case 4: deleteStaff(conn, sc); break;
            case 5:
                System.out.println("⬅ Existing");
                break;
            default:
                System.out.println("❌ Invalid choice. Try again.");
        }
    } while (choice2 != 5);
}

    private static void addStaff(Connection conn, Scanner sc) throws SQLException {
        System.out.print("Enter name: ");
        sc.nextLine(); // consume newline
        String name = sc.nextLine();
        System.out.print("Enter age: ");
        int age = sc.nextInt();
        System.out.print("Enter salary: ");
        sc.nextLine();
        int salary = sc.nextInt();

        String sql = "INSERT INTO staff (name, age, salary) VALUES (?, ?, ?)";
        try (PreparedStatement ps = conn.prepareStatement(sql)) {
            ps.setString(1, name);
            ps.setInt(2, age);
            ps.setInt(3, salary);
            int rows = ps.executeUpdate();
            System.out.println(rows + " staff(s) added.");
        }
    }

    private static void viewStaffs(Connection conn) throws SQLException {
        String sql = "SELECT * FROM staff";
        try (Statement stmt = conn.createStatement();
             ResultSet rs = stmt.executeQuery(sql)) {
            System.out.println("\nID\tName\tAge\tsalary");
            System.out.println("-----------------------------------");
            while (rs.next()) {
                System.out.println(rs.getInt("id") + "\t" +
                        rs.getString("name") + "\t" +
                        rs.getInt("age") + "\t" +
                        rs.getInt("salary"));
            }
        }
    }

    private static void updateStaff(Connection conn, Scanner sc) throws SQLException {
        System.out.print("Enter staff ID to update: ");
        int id = sc.nextInt();
        sc.nextLine();
        System.out.print("Enter new name: ");
        String name = sc.nextLine();
        System.out.print("Enter new age: ");
        int age = sc.nextInt();
        sc.nextLine();
        System.out.print("Enter new salary: ");
        int salary = sc.nextInt();

        String sql = "UPDATE staff SET name=?, age=?, salary=? WHERE id=?";
        try (PreparedStatement ps = conn.prepareStatement(sql)) {
           ps.setString(1, name);
           ps.setInt(2, age);
           ps.setInt(3, salary);
           ps.setInt(4, id);
            int rows = ps.executeUpdate();
            System.out.println(rows + " staff(s) updated.");
        }
    }
    
    private static void deleteStaff(Connection conn, Scanner sc) throws SQLException {
        System.out.print("Enter Staff ID to delete: ");
        int id = sc.nextInt();

        String sql = "DELETE FROM staff WHERE id=?";
        try (PreparedStatement ps = conn.prepareStatement(sql)) {
            ps.setInt(1, id);
            int rows = ps.executeUpdate();
            System.out.println(rows + " staff(s) deleted.");
        }
    }
    
}

    

      

    

  
    
    
     
    
    
    
    
                

